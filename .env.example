# Environment variables for Skytec Industries application
# Copy this file to .env and update values as needed

# Server configuration
PORT=3000
NODE_ENV=production

# Required in production. Set this to the initial owner password before deployment.
ADMIN_INITIAL_PASSWORD=replace-with-a-strong-password

# Optional: Configure production URL if needed
# APP_URL=https://your-domain.com
